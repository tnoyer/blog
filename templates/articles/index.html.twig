{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Liste des articles{% endblock %}

{% block body %}
    <div class="container">
        <h1 style="text-align: center; margin-bottom: 2rem;">Liste des articles</h1>
        {#
        {% for article in articles %}
            <h2><a href="{{ path('actualites_article', {'slug': article.slug}) }}">{{ article.titre }}</a></h2>
            <p>{{ article.contenu }}</p>
        {% endfor %}
        #}
        <div class="block-articles">
            {% for article in articles %}
                <div class="card" style="width: 18rem; margin-bottom: 1rem;">
                    <div class="card-body">
                        <h5 class="card-title"><a
                                    href="{{ path('actualites_article', {'slug': article.slug}) }}">{{ article.titre }}</a>
                        </h5>
                        <h6 class="card-subtitle mb-2 text-muted">{{ article.slug }}</h6>
                        <p class="card-text">{{ article.contenu }}</p>
                    </div>
                </div>
            {% endfor %}
        </div>
        <div class="navigation">
            {{ knp_pagination_render(articles) }}
        </div>
    </div>
{% endblock %}
